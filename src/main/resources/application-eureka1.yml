eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
#    register-with-eureka: false
  server:
    enable-self-preservation: true
spring:
  application:
    name: eureka
server:
  port: 8761
